/// The selectable time periods for statistics aggregation.
enum StatisticsPeriod {
  /// Statistics for the current local calendar day.
  today,

  /// Statistics for the current local calendar week (Monday–Sunday).
  week,

  /// Statistics for the current local calendar month.
  month,
}
