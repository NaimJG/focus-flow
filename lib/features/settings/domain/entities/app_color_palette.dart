/// Selectable color palette options for the application theme.
///
/// Actual seed colors live in `core/theme/app_palette_seeds.dart`.
/// User-facing labels live in the presentation layer.
enum AppColorPalette { salmon, lightBlue, lightGreen }
