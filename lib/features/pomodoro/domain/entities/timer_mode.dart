/// Represents the three modes of a Pomodoro timer cycle.
///
/// The timer alternates between [focus] sessions and breaks,
/// following the standard Pomodoro Technique sequence:
/// Focus → Short Break → Focus → ... → Long Break → repeat.
enum TimerMode { focus, shortBreak, longBreak }
